pre-commit:
  commands:
    biome-check:
      glob: "*.{js,ts,jsx,tsx,json,jsonc}"
      run: pnpm biome check --staged --no-errors-on-unmatched

pre-push:
  commands:
    typecheck:
      run: pnpm typecheck
